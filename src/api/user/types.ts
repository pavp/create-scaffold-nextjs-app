export type UserResponse = {
  id: string;
  username: string;
  applicationName: string;
  isAdmin: boolean;
};

export type User = {
  id: string;
  username: string;
  applicationName: string;
  isAdmin: boolean;
};
