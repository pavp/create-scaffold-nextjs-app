import { TimeStepOptions, TimeView } from '@mui/x-date-pickers-pro';

export const DEFAULT_TIME_PICKER_FORMAT = 'hh:mm';

export const DEFAULT_TIME_PICKER_VIEWS: TimeView[] = ['hours', 'minutes'];

export const DEFAULT_TIME_PICKER_WIDTH = '200px';

export const DEFAULT_TIME_STEPS: TimeStepOptions = { minutes: 1, hours: 1 };
